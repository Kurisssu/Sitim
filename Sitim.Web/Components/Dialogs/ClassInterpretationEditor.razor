@using Sitim.Web.Models

@*
    Reusable editor for a model's output-class interpretation: per-class name,
    clinical severity and recommendations. Mutates the supplied Rows list in place,
    so the parent dialog can read it back directly on submit.
*@

<RadzenStack Gap="0.75rem">
    <RadzenText TextStyle="TextStyle.Subtitle2" class="rz-mb-0">
        Interpretarea claselor (severitate + recomandări)
    </RadzenText>
    <RadzenText TextStyle="TextStyle.Caption" class="rz-text-tertiary-color rz-mb-0">
        Câte o intrare pentru fiecare clasă de ieșire a modelului, în ordinea indexului (0, 1, 2…).
        Severitate recomandată pentru colorare corectă: <strong>none</strong>, <strong>mild</strong>,
        <strong>moderate</strong>, <strong>severe</strong>, <strong>proliferative</strong>.
        Recomandările: câte una pe linie.
    </RadzenText>

    @if (Rows.Count == 0)
    {
        <RadzenText TextStyle="TextStyle.Caption" class="rz-text-tertiary-color">
            Nicio clasă definită. Apasă „Adaugă clasă" pentru a începe.
        </RadzenText>
    }

    @for (var i = 0; i < Rows.Count; i++)
    {
        var index = i;
        var row = Rows[index];
        <RadzenCard Variant="Variant.Outlined" class="rz-p-3">
            <RadzenStack Gap="0.5rem">
                <RadzenStack Orientation="Orientation.Horizontal" AlignItems="AlignItems.Center" JustifyContent="JustifyContent.SpaceBetween">
                    <RadzenBadge Text="@($"Clasa {index}")" BadgeStyle="BadgeStyle.Light" Variant="Variant.Flat" />
                    <RadzenButton Icon="close"
                                  ButtonStyle="ButtonStyle.Light"
                                  Size="ButtonSize.ExtraSmall"
                                  Click="@(() => RemoveRow(index))"
                                  title="Elimină clasa" />
                </RadzenStack>
                <RadzenRow Gap="0.5rem">
                    <RadzenColumn Size="12" SizeMD="7">
                        <RadzenFormField Text="Nume clasă" Variant="Variant.Outlined" class="w-100">
                            <RadzenTextBox @bind-Value="@row.Name" class="w-100" Placeholder="ex: Moderate DR" />
                        </RadzenFormField>
                    </RadzenColumn>
                    <RadzenColumn Size="12" SizeMD="5">
                        <RadzenFormField Text="Severitate" Variant="Variant.Outlined" class="w-100">
                            <RadzenTextBox @bind-Value="@row.Severity" class="w-100" Placeholder="moderate" />
                        </RadzenFormField>
                    </RadzenColumn>
                </RadzenRow>
                <RadzenFormField Text="Recomandări (una pe linie)" Variant="Variant.Outlined" class="w-100">
                    <RadzenTextArea @bind-Value="@row.RecommendationsText"
                                    class="w-100"
                                    Rows="3"
                                    Placeholder="Programare la oftalmolog în 3 luni&#10;Control glicemic strict" />
                </RadzenFormField>
            </RadzenStack>
        </RadzenCard>
    }

    <div>
        <RadzenButton Text="Adaugă clasă"
                      Icon="add"
                      ButtonStyle="ButtonStyle.Light"
                      Size="ButtonSize.Small"
                      Click="@AddRow" />
    </div>
</RadzenStack>

@code {
    /// <summary>Bound list of rows; mutated in place so the parent reads it on submit.</summary>
    [Parameter] public List<ClassInterpretationRow> Rows { get; set; } = new();

    private void AddRow() => Rows.Add(new ClassInterpretationRow());

    private void RemoveRow(int index)
    {
        if (index >= 0 && index < Rows.Count)
            Rows.RemoveAt(index);
    }
}
